package counters.minter.sdk.minter.enums

enum class BlockField {
    transactions,
    missed,
    block_reward,
    size,
    proposer,
    validators,
    evidence,
    events,
    UNRECOGNIZED;
}
