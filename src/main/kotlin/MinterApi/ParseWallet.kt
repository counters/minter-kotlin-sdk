package MinterApi

import Minter.Minter

class ParseWallet {
    fun get(/*result: JSONObject*/): Minter.Wallet? {
        val wallet = Minter.Wallet(null, "Mx000000***...")
//        println (wallet)
        return wallet
    }
}